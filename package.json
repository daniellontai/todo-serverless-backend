{
  "dependencies": {
    "@prisma/client": "^5.9.1",
    "dotenv": "^16.4.1",
    "dotenv-cli": "^7.3.0",
    "express": "^4.18.2",
    "prisma": "^5.9.1"
  },
  "scripts": {
    "start": "npm run migrate:postgres",
    "migrate:postgres": "npx prisma migrate deploy"
  }
}
